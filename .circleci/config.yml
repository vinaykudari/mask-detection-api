version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - setup-remote-docker
      - run: docker-compose build
      - run: docker-compose up

workflows:
  main:
    jobs:
      - build-and-test

